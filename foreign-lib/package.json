{
  "type": "module",
  "scripts": {
    "compile": "cd .. && spago build -p foreign-lib",
    "check": "purs-tidy check ./src ./test",
    "test": "cd .. && spago test -p foreign-lib",
    "clean": "cd .. && rm -rf ./.spago ./output ./output-es ./api/dist"
  },
  "dependencies": {
    "dayjs": "^1.11.13",
    "url-join": "^5.0.0"
  }
}